# Archived evidence + harness (from the former polymarket-edge-lab)

Raw experiment data and the measurement/validation scripts behind the findings in
`../`. The scripts here import the standalone `pm_edge` package (now folded into
`pm_trader`); they are kept as historical evidence and are NOT runnable as-is —
the live equivalents live in `pm_trader/{rewards,maker_sim,maker_live,portfolio,discovery}.py`.
